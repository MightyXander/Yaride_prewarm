/**
 * Repo-слой доступа к данным — публичный контракт.
 *
 * Реализация разбита на доменные модули в ./repo/* (issue #289: repo.ts был
 * god-file на 2400+ строк). Этот файл — тонкий совместимый реэкспорт, чтобы
 * существующие импорты (`from './repo.ts'`, включая динамические `import('./repo.ts')`
 * в telegram.ts/api.ts) продолжали работать без изменений.
 */

export * from './repo/index.ts';
