/**
 * Единый источник версии Политики обработки персональных данных (152-ФЗ).
 *
 * ВАЖНО: значение должно совпадать с версией в public/privacy.html
 * (там `window.POLICY_VERSION`). При выпуске новой редакции политики
 * меняем ОБА места согласованно — фронт шлёт эту версию в pdn_consent_version
 * при регистрации, БД хранит факт+дату+версию согласия.
 */
export const POLICY_VERSION = 'v1';
